{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "update-environment",
            "type": "shell",
            "command": "mamba env update -f environment.yml -n ${env:ENVIRONMENT_NAME} && mamba clean --all -y && mamba env export -n ${env:ENVIRONMENT_NAME} > environment.lock.yml",
            "problemMatcher": []
        }
    ]
}